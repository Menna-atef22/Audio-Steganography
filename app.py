"""
Audio Steganography - static frontend and core DSP modules
This project does not use Streamlit. Open the HTML frontend directly in a browser.
"""

import sys


def main():
    print("Audio Steganography no longer uses Streamlit.")
    print("Open frontend/index.html in a browser to access the web UI.")
    print("Use the Python API via core.encoder, core.decoder, core.audio_utils, and core.noise_test.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
